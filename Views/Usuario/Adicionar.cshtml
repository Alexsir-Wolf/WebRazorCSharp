@model WebRazorCSharp.Models.Usuario

<h2 class="text-center">@ViewBag.Message</h2>


@using (Html.BeginForm("Salvar", "Usuario", FormMethod.Post, new { id = "formNovoUsuario", name = "formNovoUsuario" }))
{

    <div class="row col-md-offset-3">
        @Html.LabelFor(v => v.Nome, new { @class = "col-sm-2 col-form-label text-right" })
        <div class="col-sm-4">
            @Html.TextBoxFor(v => v.Nome, new { @class = "form-control", @placeholder = "Nome" })
            @Html.ValidationMessageFor(v => v.Nome, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="row col-md-offset-3">
        @Html.LabelFor(v => v.Email, new { @class = "col-sm-2 col-form-label text-right" })
        <div class="col-sm-4">
            @Html.TextBoxFor(v => v.Email, new { @class = "form-control", @placeholder = "Email@email.com" })
            @Html.ValidationMessageFor(v => v.Nome, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="row col-md-offset-3">
        @Html.LabelFor(v => v.Senha, new { @class = "col-sm-2 col-form-label text-right" })
        <div class="col-sm-4">
            @Html.PasswordFor(v => v.Senha, new { @class = "form-control", @placeholder = "Senha" })
            @Html.ValidationMessageFor(v => v.Nome, "", new { @class = "text-danger" })
        </div>
    </div>

    <br />
    <div class="button text-center">
        <button class="btn btn-success " type="submit">Salvar</button>
    </div>
}


